const strings = [
    'timeoutRemaining',
    'flagrantFouls',
    'defensive3Seconds',
    'jumpShots',
    'dunks',
    'layups'
];

// slice(시작, 미만)
const sliced = strings.slice(1,3);
console.log(sliced);